{
  "dependencies": {
    "@truffle/hdwallet-provider": "^2.0.14",
    "chai": "^4.3.6",
    "dotenv": "^16.0.2",
    "web3": "^1.7.5"
  },
  "name": "publish_hashes",
  "description": "This smart contract is for storing the user data hashes and these data are linked to the user data, only unique data can be stored this is the functionality of the smart contract and user can view his data by using entering the user address..so, to test this you need: Setup Requirement We require below services/software on system for project setup.",
  "version": "1.0.0",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "devDependencies": {},
  "scripts": {
    "start": "truffle migrate",
    "reset":"truffle migrate --reset",
    "goerli":"truffle migrate --network goerli",
    "goerli-reset":"truffle migrate --network goerli --reset"
  },
  "author": "",
  "license": "ISC"
}
